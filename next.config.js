// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     i18n: {
//         defaultLocale: 'en',
//         locales: ['en', 'de'],
//       },
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
  }
   
  module.exports = nextConfig